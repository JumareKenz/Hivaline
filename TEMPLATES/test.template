/**
 * Copy this file, rename it to [NAME].test.ts(x), fill in the [PLACEHOLDERS]
 * 
 * This template is for unit tests.
 * 
 * Rules from AGENTS.md:
 * - Test: happy path, at least one edge case, at least one failure case
 * - Name pattern: [name] › [scenario] › [expected result]
 * - Mock external dependencies (sessionStorage, window.location, matchMedia)
 * - NEVER mock React itself
 */

import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';

// Import the function/component under test
import { [FUNCTION_NAME] } from '@/utils/[PATH]';
// import [COMPONENT_NAME] from '@/components/[PATH]';

// ─── MOCKS ───

// Example: mock sessionStorage
const mockSessionStorage = (() => {
  let store: Record<string, string> = {};
  return {
    getItem: vi.fn((key: string) => store[key] || null),
    setItem: vi.fn((key: string, value: string) => { store[key] = value; }),
    removeItem: vi.fn((key: string) => { delete store[key]; }),
    clear: vi.fn(() => { store = {}; }),
  };
})();

Object.defineProperty(window, 'sessionStorage', {
  value: mockSessionStorage,
});

// Example: mock matchMedia
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: vi.fn().mockImplementation((query: string) => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: vi.fn(),
    removeListener: vi.fn(),
    addEventListener: vi.fn(),
    removeEventListener: vi.fn(),
    dispatchEvent: vi.fn(),
  })),
});

// ─── TESTS ───

describe('[FUNCTION_NAME]', () => {
  beforeEach(() => {
    // Reset state before each test
    mockSessionStorage.clear();
    vi.clearAllMocks();
  });

  afterEach(() => {
    // Cleanup after each test
  });

  // ─── HAPPY PATH ───

  it('accepts valid input and returns expected result', () => {
    const result = [FUNCTION_NAME]('valid-input');
    expect(result).toBe(true);
  });

  // ─── EDGE CASES ───

  it('handles empty string gracefully', () => {
    const result = [FUNCTION_NAME]('');
    expect(result).toBe(false);
  });

  it('handles maximum length input without crashing', () => {
    const longInput = 'a'.repeat(1000);
    const result = [FUNCTION_NAME](longInput);
    expect(result).toBeDefined();
  });

  // ─── FAILURE CASES ───

  it('returns error for invalid format', () => {
    const result = [FUNCTION_NAME]('invalid!!!');
    expect(result).toBe(false);
  });

  it('throws or returns false for null input', () => {
    // @ts-expect-error Testing invalid input
    const result = [FUNCTION_NAME](null);
    expect(result).toBe(false);
  });
});

// ─── COMPONENT TEST EXAMPLE ───

describe('[COMPONENT_NAME]', () => {
  it('renders correctly with default props', () => {
    // render(<[COMPONENT_NAME] id="test" title="Test" onSelect={() => {}} />);
    // expect(screen.getByText('Test')).toBeInTheDocument();
  });

  it('calls onSelect when clicked', () => {
    const handleSelect = vi.fn();
    // render(<[COMPONENT_NAME] id="test" title="Test" onSelect={handleSelect} />);
    // fireEvent.click(screen.getByRole('button'));
    // expect(handleSelect).toHaveBeenCalledWith('test');
  });

  it('has correct accessibility attributes', () => {
    // render(<[COMPONENT_NAME] id="test" title="Test" onSelect={() => {}} />);
    // expect(screen.getByRole('button')).toHaveAttribute('aria-pressed', 'false');
  });
});
